group 'by.kotlin.groovy'
version '1.0-SNAPSHOT'

task wrapper(type: Wrapper) {
    gradleVersion = "2.12"
    distributionUrl = "http://services.gradle.org/distributions/gradle-${gradleVersion}-bin.zip"
}